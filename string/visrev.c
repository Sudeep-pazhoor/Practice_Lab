#include <stdio.h> 
#include <string.h>

char a[20]i,j=0;
int len=0;
int dot(int a[],int len){
    for(int i=0;i<len;i++){
        if(a[i]=='.'){
            start=i+1;
            j=0;
            rev(j,);
        }else{ 
        ++j;
        }
    }
}
int rev(){
    for()
}
int main() {
    printf("Input: ");
    scanf("%[^\n]s", input);
    rev(input);
    printf("Output: %s\n", input);
    len=strlen(input);
    dot(a,len);
    return 0;
}